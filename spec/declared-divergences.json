{
  "note": "Divergences from the vendor that the proxy makes on purpose. Each is data, not an exception in code: it carries the surface it applies to, the exact behavior, and why. A divergence that stops being true fails the same check that a new one does — a stale exemption hides a fixed defect and lets the next regression through. Seed of the manifest described in docs/conformance-suite-design.md §2.",
  "divergences": [
    {
      "id": "chat-function-tools-with-reasoning-effort",
      "surface": "/v1/chat/completions",
      "behavior": "The proxy accepts function tools together with a reasoning effort other than 'none' and runs them. The direct API refuses that combination for the gpt-5.6 family with 400 invalid_request_error, param 'reasoning_effort', message \"Function tools with reasoning_effort are not supported for <model> in /v1/chat/completions. To use function tools, use /v1/responses or set reasoning_effort to 'none'.\"",
      "why": "Measured 2026-08-30: gpt-5.6-terra and gpt-5.6-sol refuse it, gpt-5.5 accepts it, so the refusal is one model family's limitation on OpenAI's own chat endpoint. The Codex backend behind this proxy runs the combination, and the option rules refuse only what cannot be realized — refusing a request the backend executes would be the violation, not the parity.",
      "measuredAt": "2026-08-30",
      "evidence": "docs/conformance-matrix.md §5.5.5",
      "claim": "chat-function-tools-with-reasoning-effort"
    },
    {
      "id": "messages-output-config-task-budget",
      "surface": "/v1/messages",
      "behavior": "The proxy accepts `output_config.task_budget` ({type:'tokens', total: >= 20000}) and uses it. The direct API does not know the member and answers 400 `output_config.task_budget: Extra inputs are not permitted` — its `output_config` takes only `format` and `effort`.",
      "why": "Measured 2026-08-31: the direct API's output_config member set is {format, effort}. `task_budget` is this proxy's own extension over the Claude CLI's task budget, so it is an ADDITION to the surface rather than a difference in shared behaviour. Nothing a direct-API client sends is answered differently; a client that sends it gets the CLI knob it names. Removing it would take a capability away with no parity gain.",
      "measuredAt": "2026-08-31",
      "evidence": "docs/conformance-matrix.md §5.5.7",
      "claim": "messages-output-config-task-budget"
    },
    {
      "id": "messages-whitespace-model-is-a-400",
      "surface": "/v1/messages",
      "behavior": "The proxy answers 400 `model: String should have at least 1 character` for a whitespace-only model name. The direct API treats it as a NAME and answers 404 model-not-found.",
      "why": "Measured 2026-08-31: `model: '   '` returns 404 on the direct API, the same answer it gives any name it does not know. This proxy runs every model name on its configured backend and so has no name it does not know — it cannot mirror that 404 for one input without mirroring it for all of them, which would break its purpose. It keeps a 400 for the one shape that is plainly a slip rather than dispatching a turn for it.",
      "measuredAt": "2026-08-31",
      "evidence": "docs/conformance-matrix.md §5.5.7",
      "claim": "messages-whitespace-model-is-a-400"
    },
    {
      "id": "responses-replayed-item-ids-are-not-looked-up",
      "surface": "/v1/responses",
      "behavior": "The proxy accepts a replayed `reasoning` or hosted-tool item without checking its id, and replays it as nothing. The direct API answers 404 `Item with id '<id>' not found.` for an id it does not hold — including alongside a valid user turn — and finds the id when the item really came from its own previous response.",
      "why": "Measured 2026-08-31. Feeding `output` back as `input` is the documented way to hold a conversation on a surface that stores none, and this proxy stores none either: it has no id table to look one up in. Mirroring the 404 would mean refusing EVERY replayed item, the legitimate ones included, which would break the round trip the surface exists for. Accepting and replaying nothing is what a runtime with no reasoning slot can honestly do.",
      "measuredAt": "2026-08-31",
      "evidence": "docs/conformance-matrix.md §5.5.6-A",
      "claim": "responses-replayed-item-ids-are-not-looked-up"
    },
    {
      "id": "messages-assistant-prefill-is-not-refused",
      "surface": "/v1/messages",
      "behavior": "The proxy accepts a conversation whose last message is an `assistant` turn. `claude-sonnet-5` answers 400 `This model does not support assistant message prefill. The conversation must end with a user message.`",
      "why": "Measured 2026-08-31. The sentence names the MODEL, and assistant prefill is a capability some Claude models have and others do not. This proxy runs whatever its configured backend runs, so refusing prefill because one model refuses it would refuse a turn the backend can perform — the violation, not the parity. Same shape as `chat-function-tools-with-reasoning-effort`.",
      "measuredAt": "2026-08-31",
      "evidence": "docs/conformance-matrix.md §5.5.7",
      "claim": "messages-assistant-prefill-is-not-refused"
    }
  ]
}
