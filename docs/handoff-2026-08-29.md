# Handoff — 2026-08-29

이어서 작업하려면 이 문서만 읽으면 된다. 결정된 것은 다시 논의하지 않는다.

## 0. 같은 날 이어진 세션 (저녁) — §1·§3의 "다음 작업"은 끝났다

- 브랜치 `feat/benchmark-exchange-recorder`, `main` 위 12커밋, `pnpm test` **806/806**, 워킹트리 깨끗.
- 커밋: `f214be1` 이미지 2단계(옵션 4개를 도구 슬롯으로, 마스크는 `input_image_mask`) · `e50c4f6` `temperature`/`top_p`
  direct 동일 거절 · `48cd586` 이미지 3단계(정규식 산문 5종 제거, `asksForFlatGraphicPrompt`는 후처리 모듈로) ·
  `738dcb3` 이미지 스트림을 백엔드 첫 이벤트에서 커밋(사전 거절은 스트림이어도 HTTP 400).
- **§3의 전제가 절반만 맞았다.** 슬롯 4개는 스키마에 있지만 백엔드 이미지 모델 `gpt-image-2-codex`가
  `background: transparent`와 `input_fidelity`를 거절한다(우리 `image-2` 봉투의 출처). 증거: `pnpm probe:codex-image-tool-slots`,
  매트릭스 §5.5.1. 핸드오프의 "리서치로 확인"은 가설로 다뤄라 — 저장소에 증거 파일이 없으면 다시 잰다.
- **결정(사용자, 2026-08-29 저녁)**: ① `temperature`/`top_p`는 direct와 동일하게 거절(표면별 봉투 실측 미러링).
  ② 진단용 `--codex-image-transport app-server`는 옵션을 전달 못 해도 **일괄 거절하지 않는다** — 이미지 라우트는 대응 모델이
  없는 게 정상 조건이고 프록시는 direct에 최대한 가까운 결과를 내야 한다. 코드로 실현 가능한 것(포맷 변환·리사이즈·마스크 합성)이
  그 경로의 백로그. ③ `x_proxy_image_route` 산문 번역은 유지.
- 교차검증: Codex(`gpt-5.6-sol` xhigh)·Claude Fable 5 신선 컨텍스트, 같은 블라인드 패킷(4-A 기준·골든). 기본 경로 결함 0, 지적 3건
  전부 처리(`738dcb3`). 교훈: 리뷰 중에 워킹트리를 옮기지 마라 — 다음부턴 워크트리 분리.
- 주입량 실측: route hint 없는 요청은 전송기 프레이밍 92자/2줄 + 호출자 프롬프트 그대로. 평범한 요청과 벤치 문장이 동일.
- 부수 발견(미조치): 256×256 원본을 `size: 1024x1024`로 편집하면 백엔드가 1254×1254를 돌려준다(후처리 미적용 경로).
- 남은 백로그는 §4 그대로 + app-server 경로의 코드 실현 + Anthropic 표면 `temperature`(direct는 수용, claude CLI는 폐기 —
  계약 320행의 "hint 전달" 서술도 거짓).

### §4 "이름 충돌" 처리 — Images 모델 네임스페이스를 direct와 동일하게 (0.3.0, breaking)

direct 실측(2026-08-29)이 전제를 바꿨다: **`dall-e-2`/`dall-e-3`는 소멸**("does not exist"), `model`은 **필수**, `/v1/images/variations`는
**빈 404**, `response_format`은 어느 라이브 모델에서도 `unknown_parameter`(null 포함), `style`·generations의 `input_fidelity`도
`unknown_parameter`, edits JSON은 `images` 배열(객체)만. 라이브 이미지 모델 6개: `gpt-image-1`·`-1-mini`·`-1.5`·`gpt-image-2`·
`gpt-image-2-2026-04-21`·`chatgpt-image-latest`. 백엔드 이미지 모델 이름이 `gpt-image-2-codex`이므로 우리 라우트의 정직한 이름은 `gpt-image-2`다.

사용자 결정: **direct를 그대로 미러.** 구현: `OPENAI_IMAGE_MODELS` allowlist(`http-server.ts`), 모델 필수·타입 봉투, `image-2` 폐기,
variations 라우트 제거, `response_format`/`style` 거절, edits JSON `images` 전용, **`url` 응답 형식과 `GET /v1/images/generated/{id}`
저장소 제거**(라이브 모델 전부가 `response_format`을 거절하므로 도달 불가 → 죽은 코드). 매트릭스 §5.5.2가 실측 원자료.

**2026-08-30 이어서**: 교차리뷰 2차(Codex xhigh·Claude Fable, 핀 `91859e9`, 트리 고정) 지적 4건 반영(`d6b220d`: generations의
`input_fidelity`는 키 존재로 거절, 미지 경로는 direct처럼 빈 404+`nosniff`, 벤치 케이스 정리, 문서 잔재). 그리고 E2E 패리티 비교가
드러낸 검증 봉투 불일치를 전부 미러(정수 범위/타입·enum 문구·size·prompt(`empty_string`, 공백은 수용)·stream 타입·미지 키
`unknown_parameter`·`quality: standard/hd` 거절·검증 순서 model→미지 키→prompt→images→n). 실측 원자료는 매트릭스 §5.5.3.
**계기**: `pnpm e2e:images:parity`(`scripts/e2e-images-direct-parity.mjs`) — 같은 무효 요청을 프록시와 direct에 보내 봉투를 필드 단위로
비교, 50여 케이스 생성 없음; `--generate`를 붙이면 실제 생성 3건(생성·스트림·마스크 편집)까지. 마지막 실행: 전부 PASS.

잔여 미측정도 종결(`2d15710`): `model`/`quality`의 boolean·object·array·decimal 문구, `size` 타입, multipart(미지 키 거절·`images` 파트 안내문·
비숫자 정수 문구·enum은 JSON과 동일·`quality`가 `output_compression`보다 먼저). 패리티 계기는 JSON 62 + multipart 11 케이스, 전부 PASS.
**Anthropic 표면**(§4 백로그): `temperature`/`top_p`(0..1, null은 생략 아님)/`top_k`(정수)를 direct 문구로 검증하고 유효값은 수용·미적용
(claude CLI에 노브 없음, 에코 필드 없음). `NormalizedRequest.temperature`는 소비자가 없어 제거. 매트릭스 §5.5.4. 남은 Anthropic 발산은
A-37(미지 최상위 필드 — direct는 "Extra inputs are not permitted", 프록시는 무시; 전체 키 집합 미측정이라 보류).

**이미지 모델 핀 A/B (2026-08-30, §4 백로그 종결)**: `--codex-image-model` gpt-5.5 vs gpt-5.6-terra, 같은 8행(생성 5·스트림 1·편집 2),
repeats 1, judge gpt-5.5. 둘 다 8/8 성공(terra가 실제 이미지를 돌려주는 것 확인). 판정 점수 평균 95.3 vs 95.1(n=7, min 92 vs 91,
median 96 = 96) — A/A 변동폭 안. 지연 median 15.0s vs 17.4s(terra가 +2.3s). **결론: 핀은 gpt-5.5 유지.** 원자료
`bench-results/ab-image-pin-gpt5.5-20260830.json`·`ab-image-pin-gpt5.6-terra-20260830.json`.
**벤치 이미지 스위트(§4)**: 오류 행 전부 양쪽 타깃에서 통과(11/11). `background_transparent_unsupported`는 direct terra가 transparent를
수용·생성하므로 proxy 전용 행으로 전환(백엔드 능력 격차 기록). `variation`·`generationUrl` 삭제. 남은 큰 실행은 image-realistic 전체
재측정(생성 ~20행 × 2 타깃 + judge)뿐.

**app-server 경로 코드 실현 (2026-08-30, 사용자 결정: sharp 도입)**: `src/proxy/image-realize.ts` — 마스크 합성(원본 치수, 마스크 투명=생성/불투명=원본)
→ `size`(cover) → `background: opaque` 평탄화 → `output_format`/`output_compression` 인코딩. `transparent`·`input_fidelity`는 백엔드 이미지 모델의
봉투로 턴 전에 거절(기본 경로와 동일). `sharp`는 유일한 런타임 의존성이며 그 경로에서 실현할 일이 있을 때만 동적 import(다른 런타임은 무의존).
`package-adapter.mjs`의 "런타임 의존성 금지" 검사는 allowlist `['sharp']`로 완화. 마스크 합성은 원격 URL 원본/마스크를 못 받는다(프록시는 자체
아웃바운드 요청 금지 → 400 안내). §4 백로그 전부 종결.

**소비자 통보 필요**: `approval-export-a0`는 `model: "image-2"` → `"gpt-image-2"`, 본문의 `response_format` 제거, `quality`에
`standard`/`hd`를 쓰고 있다면 `medium`/`high`로. 벤치 스크립트의 이미지 오류 행은 2026-08-30에 정리·재실행 완료(위 참조).

## 1. 지금 어디까지 와 있나

- `main` = `08ccea0` (PR #12 머지 완료, 열린 PR 없음)
- 작업 브랜치 = `feat/benchmark-exchange-recorder`, 커밋 7개, 워킹트리 깨끗
- `pnpm test` **798/798**, `pnpm pack:adapter` 통과
- 패키지 버전 0.2.1 (소비자 `approval-export-a0`는 이미 전환 완료)

브랜치의 커밋(오래된 순):

| 커밋 | 내용 |
|---|---|
| `94cf99f` | 벤치 러너에 교환 기록기 부착 (적합성 이행 1단계) |
| `180df15` | direct API Tier A 프로브 12회 실행 |
| `8314424` | 캡처를 읽는 첫 적합성 검사 + `/v1/responses`의 `[DONE]` 제거 |
| `5112a36` | claude 사용자 설정 격리를 기본값으로 |
| `59f5731` | 호출자 텍스트가 도구 이력으로 오독되던 결함 수정 |
| `f8f2c26` | 클라이언트가 안 보낸 `text.verbosity` 주입 중단 |
| `200c9dc` | 이미지 옵션 중복 산문 제거 (이미지 1단계) |

## 2. 확정된 원칙 — 재논의 금지

**어댑터는 CLI의 인터페이스를 API와 같게 만드는 도구다.** 같은 모델이라도 CLI와 API는 인터페이스가
달라 응답 특성이 달라지고, 그 차이를 없애는 것이 어댑터의 존재 이유다. 다만 그 일은 이렇게 한다:

1. **환경 제약** — 그 동작이 애초에 불가능하게 (자식의 HOME·cwd·설정 소스·MCP)
2. **런타임 노브** — 네이티브 플래그·파라미터로 끄기
3. **상쇄 래퍼** — 끌 수 없으면 **응답 경로에서 코드가** 잔재를 제거
4. ~~프롬프트 주입~~ — **금지**

어댑터의 일은 두 방향이다: **빼기**(CLI 래퍼 벗겨내기·우회)와 **더하기**(API에만 있는 기능 구현).
더하기의 올바른 형태는 **제약은 런타임 노브가 걸고, 번역은 프록시 코드가 한다**.

### 옵션 구현 규칙 (2026-08-29 확정)

**옵션은 약속이고 프롬프트는 부탁이다.** 약속을 부탁으로 구현하면 어댑터가 클라이언트에게 거짓말을 한다.

| 상황 | 해야 할 것 |
|---|---|
| 슬롯이 있다 | 슬롯으로 보낸다. 산문 금지 |
| 슬롯이 없고 코드로 검증 가능 | 코드로 구현 (`n`이 이미 그렇다 — 병렬 턴) |
| 슬롯도 없고 검증도 안 된다 | **거절하거나 미지원으로 문서화.** 부탁으로 구현하지 않는다 |
| 호출자가 요청하지 않았다 | **넣지 않는다** |

**절대 하지 말 것: 수용해놓고 전달하지 않으면서 응답에는 적용됐다고 에코하는 것.**

### 판정 기준 (2026-08-29 확정)

- **분량은 판정 축이 아니다.** 모델은 비결정적이고, A/A 대조에서 vendor 자신의 run 간 변동이
  중앙값 13.4%(최대 30%)로 관측 격차와 같은 크기였다. 판정은 **형식과 내용 품질**로 하고, 분량은
  그 둘이 통과한 뒤에도 크고 일관된 차이가 남을 때만 조사 단서로 쓴다.
- 검증 권위는 손으로 쓴 문항이 아니라 **API 표면 자체**다. 설계는
  `docs/conformance-suite-design.md`, 원자료는 `docs/conformance-matrix.md`(148행).
- LLM 판정은 게이트에서 내려왔다. 그것이 런타임을 문항에 맞춰 조정되게 만든 유인이었다.

## 3. 바로 다음 작업 — 이미지 2단계

`src/proxy/image2-via-gpt55.ts`의 산문 주입을 위 규칙으로 정리한다. 1단계(중복 4건)는 끝났다.

**2단계 범위(위 규칙 확정으로 넓어졌다):**

1. **슬롯 배선 4건** — `background`, `moderation`, `input_fidelity`, `mask`(= `input_image_mask`).
   네 슬롯 모두 라이브 Codex 백엔드 스키마에 **존재한다**(2026-08-29 리서치로 확인, 이전 감사의
   "슬롯 없음"은 오판이었다). 지금은 안 보내고 산문으로만 말한다.
2. **거짓 에코 제거** — `codex-backend-transport.ts:893`·`699`·`716`이 `background`를 응답에
   되돌려 준다. 도구에는 안 보내면서. `defaults-diff`가 찾은 `temperature` 거짓 에코도 같은 부류다.
3. **미지원 선언** — 슬롯도 검증도 없는 것(`style`, negative prompt 개념 자체)은 배선이 아니라
   거절 또는 미지원 문서화.

배선 뒤 해당 산문을 지운다. 부수 효과로 `gpt-image-2` 요청의 `background: transparent`가
**수용 후 조용히 폐기**되던 것이 고쳐진다.

**3단계(그 다음)** — regex 계열 5개 제거: `geometryConstraints`,
`targetedEditPreservationConstraints`, `flatGraphicConstraints`, `negativePromptConstraints`,
`editConstraints`의 일반 절반. 전부 **호출자 프롬프트를 정규식으로 읽어** 발동하고 API 필드가 없으며
이미지 벤치 문장(`A simple flat red square centered on a white background. No text.`)에 걸린다.
주의: `flatGraphicConstraints`는 코드 쪽 후처리(`flat-image-postprocess.ts`)도 같은 술어
(`asksForFlatGraphicPrompt`)로 게이트하므로 산문만 지우면 안 된다.

현재 주입량(실측): 평범한 요청 **1줄/219자** vs 벤치 문장 **12줄/1,630자**, 벤치 편집 **13줄/1,925자**.
3단계까지 끝나면 이 차이가 사라진다.

## 4. 남은 백로그

| 항목 | 상태 |
|---|---|
| 이미지 2·3단계 | 위 참조 |
| **이름 충돌** | `gpt-image-2`가 실재 vendor 모델인데 우리 라우트도 `image-2`. 모델 allowlist가 없어 `gpt-image-2`로 온 요청이 **경고 없이** 로컬 라우트로 가고 vendor에 도달하지 않는다. 결정 필요 |
| **이미지 모델 핀** | `codexProxy.imageModel`이 `gpt-5.5`(텍스트는 `gpt-5.6-terra`). `gpt-5.6-terra`가 `image_generation` 도구 선언을 수용하는 것은 무료 프로브로 확인, 실제 이미지 반환은 미검증. 3단계 뒤에 A/B |
| **P2** | claude 상시 경로에 네이티브 스키마 채널. OpenAI 형태 json 모드와 tool 판단 스키마가 지금 무제약으로 나간다 |
| 적합성 이행 2·4~7단계 | `docs/conformance-suite-design.md` §7. 1·3단계는 완료 |
| runtime capability 도구 | **유지보수 모드.** 결함 11건을 `docs/runtime-capability-update-playbook.md`에 기록만 하고 고치지 않기로 함(3라운드에서 수정 2건당 회귀 1건) |

## 5. 이 세션에서 반복해서 값을 한 규율

이게 가장 중요하다. 오늘 잡은 결함의 상당수가 **내 수정이 만든 것**이었고, 전부 같은 부류였다:
**아무것도 아닌 것과 비교해서 통과하는 검사.**

- **모든 게이트를 변이로 양방향 확인한다.** "고쳤다" 뒤에 반드시 되돌려서 빨간불을 본다.
  오늘 이걸로 잡은 것: 판정 역전, 잘못 겨눈 단언(추출기를 비워도 초록), 빠뜨린 return.
- **기대와 일치하는 관찰이 계기를 의심할 때다.** 프로브가 `timeout` 부재로 실행조차 안 됐는데
  파이프의 grep 종료 코드를 보고 성공으로 읽은 일, 실패 행 패턴이 안 맞아 "FAIL 0"으로 보고한 일이
  각각 있었다.
- **파이프 종료 코드는 마지막 단계 것이다.** 테스트 대상의 상태를 직접 받아라.
- **`pgrep -f`는 자기 자신을 매칭한다.** 프로세스 판정은 PID로.
- **대조군을 둔다.** 재현 하네스에 "정상 케이스"를 함께 넣지 않으면 하네스 고장과 결함을 구분 못 한다.
  오늘 P0 재현 하네스는 두 번 틀렸다(백엔드 인터페이스 오류로 호출조차 안 됨, 노멀라이저 층만 봄).
- **리뷰 지적을 그대로 받지 않는다.** 세 라운드 21건 중 일부는 이미 고쳐진 상태를 본 것이었고, 일부는
  판정이 절반만 맞았다. 코드에서 재확인한 뒤에 고친다.
- **macOS에 `timeout`이 없다.** 도구 자체 타임아웃을 쓴다.

## 6. 증거와 아티팩트

| 위치 | 내용 |
|---|---|
| `spec/captures/` | 적합성 검사가 읽는 캡처 4개(direct/proxy × chat/responses 스트림). 각각 원본 run id와 sha256을 지닌다 |
| `spec/declared-divergences.json` | 의도된 발산 목록(현재 비어 있음). 매니페스트의 씨앗 |
| `artifacts/api-captures/` | 벤치 실행이 남긴 교환 기록 (gitignored) |
| `artifacts/direct-api-captures/` | Tier A 프로브 원본 (gitignored) |
| `bench-results/aa-control-terra-sonnet5-20260828.json` | A/A 대조 — 분량 축 판정의 근거 |
| `bench-results/bench-quality-isolated-terra-sonnet5-20260828.json` | 격리 + 새 모델 기준선 |
| `docs/conformance-matrix.md` §5.5 | Tier A 실측 결과 |

**모델**: OpenAI `gpt-5.6-terra`, Anthropic `claude-sonnet-5`(둘 다 라이브 목록에서 실측 확인).
judge는 `gpt-5.5` 고정 — 대상이 바뀌는 run에서 계기까지 바꾸지 않기 위해.

**API 키**는 `.env`(gitignored)에 있다. `set -a; . ./.env; set +a`로 로드.

## 7. 유용한 명령

```bash
pnpm test                                    # 798/798
pnpm bench:api -- --suite=contract-smoke --targets=proxy-codex --repeats 1 \
  --codex-model gpt-5.6-terra --output /tmp/smoke.json    # 교환이 자동 기록된다
node scripts/probe-direct-api.mjs --only P-2 # vendor 프로브 하나만
node scripts/promote-capture.mjs --name <fixture> --from artifacts/api-captures \
  --url /v1/responses --must-contain "response.completed"  # 캡처를 spec/으로 승격
pnpm catalog:runtime -- --fail-on-stale      # 카탈로그 게이트(플래그 없으면 항상 exit 0)
```

## 8. 열린 질문

- 이름 충돌을 어떻게 풀 것인가 — `gpt-image-*`를 거절할 것인가, 명시적으로 라우팅할 것인가,
  라우트 이름을 바꿀 것인가
- 이미지 모델 핀 이동 여부 (생성 호출 A/B 필요, 비용 있음)
- 적합성 스위트 2단계(매니페스트)를 언제 시작할 것인가 — 지금은 캡처를 읽는 검사가 하나뿐이다
