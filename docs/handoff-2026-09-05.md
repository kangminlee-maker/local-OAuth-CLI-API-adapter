# Handoff — 2026-09-05 (SSOT)

이 문서 하나로 이어서 작업한다. 이전 핸드오프(`docs/handoff-2026-08-30.md`)는 이력이며, 이 문서와 다르면 이 문서가 맞다.
**결정된 것(§2)은 다시 논의하지 않는다.** 작업은 §4의 순서대로 하고, 각 단계의 "done when"과 대조한 뒤에만 완료라고 말한다.

## 1. 지금 어디까지 와 있나

- `main` = __MAIN__ (PR #15 머지, 2026-09-05). 열린 PR 없음. 브랜치 `fix/adapter-conformance-r11-r13`은 머지됨.
- 패키지 **0.4.0** (breaking — 새 거절이 늘었다: 요청 경계 400 26건, 응답 경로 502(래퍼 호출 인자가 JSON 객체가 아니거나 strict 스키마 밖),
  `holdToolTurnsUntilComplete` 기본 on). 오프라인 `pnpm test` **1845/1845**, 라이브 direct 패리티 **ALL PASS 463/463**(`review-artifacts/stage2/parity-r21-final.log`, d17e06f), `pack:adapter`·설치본 E2E 통과.
- 런타임 의존성 **2개: `sharp`, `ajv`**(8.20.0, draft 2020-12, `strict:false`·`validateFormats:false`, 스키마별 인스턴스 격리).
- 모델: OpenAI `gpt-5.6-terra`(텍스트 실측), Codex 이미지 핀 `gpt-5.5`, Anthropic `claude-sonnet-5`. 리뷰 좌석: codex `gpt-5.6-sol` ultra + Claude Fable 5.1(frontier).
- 리뷰: 텍스트 표면 캠페인 22라운드. 14라운드부터는 매 라운드가 **직전 수정 커밋**을 대상으로 두 좌석(codex + Fable, 블라인드 패킷, 라이브 호출 금지)이 봤다.
  22라운드(d17e06f 리뷰)는 Fable 4건 + codex 3건(부분 보고 — 분류기 중단, 20라운드에 이어 두 번째)에서 공유 1건을 뺀 6건을 찾았고(그중 이 수정이 유발한 것 2: 2차 회귀 strip, 절단 투영의 키 미검증), 같은 날 고쳤다(__R22FIX__). 그 커밋은 23라운드에서 __R23__.
- **소비자 통보**: `~/Documents/approval-export`(0.2.1 tgz, git 아님, 세션 `approval-export-5e`)와 Workbench-TA_AX(Images + 버퍼 messages) — 0.3.0 통보도 나가지 않았고,
  0.4.0 통보가 §4-1이다. 프록시·봇 재기동은 **사용자 결정**(이 세션도 건드리지 않았다).

## 2. 확정된 원칙 — 재논의 금지

**어댑터는 CLI의 인터페이스를 API와 같게 만드는 도구다.** 수단은 이 순서: ① 환경 제약 ② 런타임 노브 ③ 응답 경로의 코드
④ ~~프롬프트 주입~~ **금지**. **옵션은 약속이고 프롬프트는 부탁이다.**

| 상황 | 해야 할 것 |
|---|---|
| 슬롯이 있다 | 슬롯으로 보낸다. 산문 금지 |
| 슬롯이 없고 코드로 실현·검증 가능 | 코드로 실현 |
| 둘 다 아니다 | direct가 거절하면 같은 봉투로 거절, direct가 수용하면 수용하되 "미적용"을 `docs/conformance-matrix.md`에 **선언** |
| 호출자가 보내지 않았다 | 넣지 않는다 |

**절대 하지 말 것: 수용해놓고 전달하지 않으면서 응답에 적용됐다고 에코하는 것.**

**응답 경로의 백스톱은 거절하지 수리하지 않는다.** 모델이 쓰지 않은 답으로 고쳐 쓰지 않는다 — 앞 공백 strip, 객체형 인자의 stringify,
`{"input": …}` 감싸기, parse+stringify 재직렬화 전부가 "수리"였고 전부 지웠다. **런타임이 쓴 바이트가 답이다** — 모든 표면, 두 경로 모두.
JSON 값 표면(Messages `input`)에서는 `RawJson`/`stringifyJson`으로 바이트를 접합한다.

**판정할 수 없는 것은 통과시킨다(`unjudged`)**: 컴파일 안 되는 스키마, `$async`, 미해결 `$ref`, 그리고 double이 정확히 못 담는 숫자를 실은
텍스트의 위반(유효 숫자 기준 — 0 패딩은 부정확이 아니다). 스키마 강제는 클라이언트가 약속을 건 곳에서만(`strict: true`, Messages 구조화 출력).

**출력 한도 예외**: 런타임이 `max_tokens`를 보고한 턴은 어떤 완료 규칙도 판정하지 않고, direct가 전달하는 모양으로 전달한다
(Chat `length`, Responses `incomplete`, Messages `max_tokens` + 본문엔 완성 멤버·스트림엔 마지막 블록만 열어 둠). app-server는 stop reason이 없어 조각을 항상 거절(선언).

**표는 계기 뒤에 쓴다.** 미러의 첫 단계는 direct 실측 행 추가(`review-artifacts/stage2/report.md` M5–M9). 두 표본은 "결정적"의 증거가 아니다
(M8 정정: Chat 빈 `tools`+`required`는 생성 단계에서 비결정).

**두 경로는 같은 답을 해야 한다** — 스트림 vs 버퍼가 이 저장소의 반복 결함 형태. 완료 후 거절은 스트림에선 바이트 뒤 in-band error, 버퍼에선 502가 선언된 모양.

**결함 수정에 기능 개발 금지. 수정이 수정을 부르면 설계 과제.**

## 3. 이 세션들(09-03 ~ 09-05)에서 결정·완료한 것

- §7 잔여행 종결(4bbd033): 클라이언트 스키마를 완료 시점에 강제(ajv), 절단 예외, 행 11·12 실측 미러, 행 13 측정·선언.
- 18~21라운드 리뷰 실결함 9+14+7+4건 수정(87ac1c5, 5813b1e, 73d99cc, f334978+d17e06f). 커밋 메시지가 각 라운드의 지적과 조치를 적는다.
- 선언된 차이(매트릭스 §7·행 12·16·28·30): app-server 조각 거절, `json_schema`+`tools` 미강제, Chat 빈 `tools`+`required` 고정 500, Responses 내장 도구 타입 거절,
  strict-스키마 검증군(`invalid_function_parameters`) 미러 안 함, 행 13 에코.
- 미측정(리뷰가 이름 붙인 것): Messages 이력 리더의 `tool` 폴백, Messages 비문자열 도구 이름 문구, boolean-root 스키마, 절단 시 Responses 텍스트 아이템 status,
  다중 호출 절단 턴의 아이템별 status, 외래 위치 도구 멤버의 direct 봉투 일부, 네이티브 전송기가 비JSON 인자를 실제로 내는지.

## 4. 남은 작업 — 이 순서대로

### 4-1. 소비자 통보 (0.4.0) — 즉시
`artifacts/local-oauth-cli-api-adapter-0.4.0.tgz`(`pnpm pack:adapter`로 재생성)와 함께 변경 요지: 새 400(요청 경계)·502(응답 경로) 거절 목록은
`docs/conformance-matrix.md` §7과 `docs/api-interface-contract.md`의 차이 표. approval-export의 요청 형태(`/v1/messages` claude, `/v1/responses` codex `store:false`)는
새 거절에 걸리지 않는다 — 도구 정의에 빈 이름·외래 위치 멤버가 없고 strict를 걸지 않는 한. **done when**: 두 소비자에게 통보 메시지가 전달되고, 교체 여부의 결정이 기록됨.

### 4-2. 미측정 항목의 실측 (작음, 선택)
§3의 미측정 목록을 `stage2/r18-measure.mjs` 양식으로 잰다. **done when**: 각 항목이 report.md에 날짜 붙은 행으로 있고, 미러하거나 선언함.

### 4-3. 적합성 3단계 잔여 — 재생 경로 (설계 §7-3) → 4-4. PR 게이트 (§7-4) → 4-5. 옛 단언 이관 (§7-5) → 4-6. 증거 채우기 (§7-6)
이전 핸드오프 4-6~4-9 그대로. **done when**은 그 문서의 것.

### 4-7. 벤치 image-realistic 재측정 (선택) → 4-8. 품질 실험 교체·벤치 러너 은퇴 (§7-7)
A/A 잡음 바닥을 먼저 재고 게이트를 켠다.

### 유지보수 모드
runtime capability 도구 — 결함 11건은 `docs/runtime-capability-update-playbook.md`에 기록만.

## 5. 반복해서 값을 한 규율

- **리뷰 라운드는 직전 수정을 본다.** 수정은 새 표면이고, 15·16·20·21라운드가 실제로 직전 수정이 만든 결함을 찾았다(caused와 surfaced를 구분해 적는다).
- **변이는 대상 존재부터 단언한다** — 앵커 카운트 ==1, 컴파일, 관측 가능한 변화, 프로브가 무변이 코드에서 판별함을 먼저 보인다. macOS엔 `timeout`이 없어 조용히 빈 출력이 났다.
- **측정과 일치하는 판독일수록 계기를 의심한다.** 두 표본의 500을 "결정적"이라 적었고, 최종 parity가 그것을 깼다.
- **한 멤버를 고칠 때 같은 리더의 형제 멤버를 전수 확인한다.** `strict`만 자기 위치로 스코프하고 `name`/`parameters`는 교차 판독으로 남겼다.
- **수리 함수는 판별을 부수효과로 겸하고 있을 수 있다.** 네이티브 전송기의 수리를 지우자 "완료 신호를 낼지"의 판별이 사라져 회귀 1건 — 명시적 `parsesAsJson`으로 살렸다.
- **codex 좌석은 OpenAI 콘텐츠 분류기에 걸릴 수 있다**(20라운드 두 번 중단, 같은 패킷이 다른 라운드에선 통과). 기록하고 Claude 좌석으로 라운드를 마친다.
- **라이브 parity는 dist 변이와 동시에 돌리지 않는다.** 산출물은 `review-artifacts/`(스크래치패드는 통째로 비워진다).

## 6. 증거와 아티팩트

| 위치 | 내용 |
|---|---|
| `docs/conformance-matrix.md` §7, 행 8·12·16·28·30·A-2 | 래퍼 읽기 한 번, 선언된 차이, 절단 투영 — 전부 날짜 붙은 direct 실측 |
| `docs/api-interface-contract.md` | 현재 계약(차이 표 "Client schema enforced at completion", Messages 도구 인자 바이트 문단) |
| `spec/conformance.json` | 매트릭스에서 생성(`pnpm conformance:manifest`), 테스트가 매트릭스 SHA를 고정 |
| `review-artifacts/stage2/report.md` M5–M9(+M8 정정) | direct 봉투 실측 원자료(절단 호출 3표면, 요청 경계 26건, 외래 위치 멤버, 빈 tools+required 분포) |
| `review-artifacts/r14…r22-{codex,fable}/REPORT.md`, `packet-r*.md`, `stage2/mutants-r*.log` | 라운드별 리뷰 보고·패킷·변이 로그 (untracked) |
| `review-artifacts/stage2/parity-r21-final.log` | 마지막 라이브 parity 실행 |

**API 키**는 `.env`(gitignored): `set -a; . ./.env; set +a`. 절대 에코하지 않는다.

## 7. 명령

```bash
pnpm test                                   # 1845/1845 (build + runtime boundary + node --test)
pnpm pack:adapter && pnpm test:e2e:adapter  # 0.4.0 tgz + fresh consumer 설치 E2E
pnpm e2e:text:parity                        # 프록시 vs direct 봉투 패리티 463행 (거절 행만, 무료)
pnpm conformance:manifest                   # 매트릭스 → spec/conformance.json
node --test --test-concurrency=2 test/*.test.mjs 2>&1 | grep -aE '^ℹ (pass|fail)'   # tail 금지
```

리뷰 라운드 절차: `git archive <commit>` 사본 + `node_modules` 심링크 + `PACKET.md`(질문 셋, 제약, 보고 계약) + `REVIEW_DIFF.patch` + `REVIEW_COMMITS.txt` +
`MEASUREMENTS.md`(= report.md) + `git init` 기준선 + `pnpm -s build`. codex: `/opt/homebrew/bin/codex exec -C <copy> -m gpt-5.6-sol -s workspace-write
-c model_reasoning_effort=ultra -o <out> < <prompt>`; Claude: `Agent`(frontier, 모델 지정 없음) 신선 컨텍스트. 두 좌석 모두 라이브 API·LLM CLI 호출 금지.
